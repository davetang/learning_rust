// functions defined using `fn`
// blocks defined by curly braces
// Rust will automatically start in main
fn main() {
    // println! (print line) is a macro and prints to STDOUT
    // a macro is code that write code
    println!("Hello, world!");
}

